# Optional Docker Compose overrides
# Copy to .env and adjust values when needed.

# Host port mapped to container port 8099
HOST_PORT=8099

# Container name used by docker compose
COMPOSE_CONTAINER_NAME=alpedhuzes-aveleijn

# Restart policy for compose service
RESTART_POLICY=unless-stopped

# Runtime mode inside container
NODE_ENV=production
